
from setuptools import setup

setup(name="simple_dpay_client",
      version          = 0.1,
      description      = "A minimalist RPC client for dPay",
      url              = "https://github.com/dpays/dpay-simple-cli",
      author           = "dPay DAO",
      packages         = ["simple_dpay_client"],
      install_requires = [],
      entry_points     = {},
    )
